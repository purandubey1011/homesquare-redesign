import { QuoteBandSection } from "../site/PageSections";

const section = {
  type: "quoteBand",
  heading: "A destination where category relevance and business practicality work together.",
  body: "Home Square is designed to help brands operate inside a commercially sharper environment with better audience fit, stronger visibility and more useful amenities."
};

const DestinationCategory = () => <QuoteBandSection section={section} />;

export default DestinationCategory;
