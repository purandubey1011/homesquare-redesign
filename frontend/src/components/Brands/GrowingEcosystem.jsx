import { QuoteBandSection } from "../site/PageSections";

const section = {
  type: "quoteBand",
  heading: "A growing ecosystem of relevant brands.",
  body: "When brands serve the same customer journey, the entire destination becomes stronger."
};

const GrowingEcosystem = () => <QuoteBandSection section={section} />;

export default GrowingEcosystem;
