import { QuoteBandSection } from "../site/PageSections";

const section = {
  type: "quoteBand",
  heading: "Developer Philosophy",
  body: "Create commercial spaces that are practical, future-ready, experience-led and built around how people actually discover, evaluate and choose brands."
};

const DeveloperPhilosophy = () => <QuoteBandSection section={section} />;

export default DeveloperPhilosophy;
