import { QuoteBandSection } from "../site/PageSections";

const section = {
  type: "quoteBand",
  heading: "Why a one-stop destination matters.",
  body: "When categories that serve the same customer journey are brought together, buying becomes easier and business discovery becomes stronger."
};

const OneStop = () => <QuoteBandSection section={section} />;

export default OneStop;
