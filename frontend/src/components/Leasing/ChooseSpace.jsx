import { QuoteBandSection } from "../site/PageSections";

const section = {
  type: "quoteBand",
  heading: "Choose a space that matches your business format.",
  body: "Home Square supports multiple business models, from large product-led showrooms to offices, studios, service hubs and experience centres."
};

const ChooseSpace = () => <QuoteBandSection section={section} />;

export default ChooseSpace;
